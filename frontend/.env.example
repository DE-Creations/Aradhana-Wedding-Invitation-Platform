# Backend API origin.
# Leave empty in development — Vite proxies /api and /storage to
# http://localhost:8000 (see vite.config.js).
# In production set this to your Laravel backend origin, e.g.
# VITE_API_URL=https://api.yourdomain.com
VITE_API_URL=
